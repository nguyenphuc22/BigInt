name: C++ CI

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install dependencies
      run: sudo apt-get install -y g++
    - name: build and run 1
      run: |
        cd HCMUS-BigInt/build
        cmake CMakeLists.txt
        make
        ls
        
    - name: build and run 2
      run: |
        cd HCMUS-BigInt/build
        cmake CMakeLists.txt
        make
        ls
